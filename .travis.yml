language: python
python:
  - "3.6"
install:
  - pip install coverage
  - pip install coveralls
script:
  - coverage run --source=python_solutions tests.py
  - coverage report -m
  - mkdir build && cd build && cmake .. && make && cd .. && ./buld/tests
after_success:
  - coveralls